import styled from 'styled-components';

const Board = styled.div`
  text-align: center;
  margin: auto;
  width:480px;
  border:10px solid var(--color-secondary);
  box-shadow:inset 0px 0px 180px -15px var(--color-accent);
`;

export default Board;
